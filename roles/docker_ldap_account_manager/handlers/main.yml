---
- name: Restart docker ldap account manager compose
  ansible.builtin.systemd:
    name: docker-ldap-account-manager
    state: restarted
    daemon_reload: true
  when: docker_ldap_account_manager.apply | bool
