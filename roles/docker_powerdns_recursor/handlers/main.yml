---
- name: Restart docker powerdns recursor compose
  ansible.builtin.systemd:
    name: docker-powerdns-recursor
    state: restarted
    daemon_reload: true
  when: docker_powerdns_recursor.apply | bool
