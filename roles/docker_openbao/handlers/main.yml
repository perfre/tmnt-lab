---
- name: Restart docker openbao compose
  ansible.builtin.systemd:
    name: docker-openbao
    state: restarted
    daemon_reload: true
  when:
    - docker_openbao.apply | bool
    - not ansible_check_mode
