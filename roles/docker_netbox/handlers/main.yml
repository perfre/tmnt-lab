---
- name: Restart docker netbox compose
  ansible.builtin.systemd:
    name: docker-netbox
    state: restarted
    daemon_reload: true
  when:
    - docker_netbox.apply | bool
    - not ansible_check_mode
